# 🔄 ניהול גרסאות - מדריך מהיר

## 📌 איך לעדכן מספר גרסה

### מיקום מרכזי - קובץ index.html
**שורה ~920 (בתחילת תגית `<script>`):**

```javascript
// ========== VERSION MANAGEMENT ==========
const APP_VERSION = "3.4";  // ★ נהל את מספר הגרסה רק כאן ★
```

### עדכון אוטומטי
מספר הגרסה מתעדכן אוטומטית ב:
- ✅ כותרת המחשבון (תצוגת משתמש)
- ✅ Console logs (אם מוסף)
- ✅ ייצוא נתונים (אם משתמש בפונקציית export)

### עדכון ידני נדרש ב:

**1. Service Worker (sw.js):**
```javascript
const CACHE_NAME = 'salary-calc-v3.4';  // ← עדכן כאן
```

**2. Manifest (manifest.json):**
```json
{
  "short_name": "מחשבון שכר v3.4",
  "name": "מחשבון שכר וחיסכון פנסיוני v3.4",
  ...
  "version": "3.4"
}
```

## 🎯 תהליך עדכון גרסה מלא

### שלב 1: עדכן קבוע הגרסה
```javascript
const APP_VERSION = "3.5";  // גרסה חדשה
```

### שלב 2: עדכן Service Worker
```javascript
const CACHE_NAME = 'salary-calc-v3.5';
```

### שלב 3: עדכן Manifest
```json
{
  "short_name": "מחשבון שכר v3.5",
  "name": "מחשבון שכר וחיסכון פנסיוני v3.5",
  "version": "3.5"
}
```

### שלב 4: נקה cache בדפדפן
- Chrome: `Ctrl+Shift+Delete` → בחר "Cached images and files"
- או: DevTools → Application → Clear storage

## 📝 מוסכמות מספור גרסאות

### פורמט: `MAJOR.MINOR`

**MAJOR (מספר עיקרי):**
- שינוי ארכיטקטורה
- שינוי מהותי בפונקציונליות
- שינוי שובר תאימות לאחור
- דוגמה: `3.0` → `4.0`

**MINOR (מספר משני):**
- תכונה חדשה
- תיקון באגים
- שיפורים בממשק
- דוגמה: `3.4` → `3.5`

## 🐛 דוגמאות לשינויי גרסה

### גרסה 3.4 (תיקון)
```
✅ תיקון: גלילה לכותרות במחשבונים
✅ תיקון: חישוב שכר נוסף בסיכום שנתי
✅ שיפור: ניהול מרכזי של מספר גרסה
```

### גרסה 3.5 (תכונות חדשות - דוגמה)
```
✨ חדש: מחשבון קצבת זקנה
✨ חדש: ייצוא PDF
🔧 שיפור: ביצועים משופרים
```

### גרסה 4.0 (שינוי מהותי - דוגמה)
```
🚀 ארכיטקטורה חדשה
🎨 עיצוב מחדש מלא
💾 מעבר ל-IndexedDB במקום localStorage
```

## 🔍 בדיקת גרסה

### בקונסול:
```javascript
console.log('Current version:', APP_VERSION);
```

### בקוד:
```javascript
if (APP_VERSION === "3.4") {
    // עשה משהו ספציפי לגרסה זו
}
```

## ⚠️ טיפים חשובים

1. **תמיד עדכן את שלושת הקבצים** - index.html, sw.js, manifest.json
2. **שמור גרסה במערכת ניהול גרסאות** (Git tags)
3. **תעד שינויים** ב-CHANGELOG (אם קיים)
4. **בדוק לפני פרסום** - וודא שהגרסה מוצגת נכון
5. **נקה cache** - אחרי עדכון גרסה תמיד נקה cache

## 📚 קבצים רלוונטיים

```
project/
├── index.html          ← const APP_VERSION = "3.4"
├── sw.js              ← const CACHE_NAME = 'salary-calc-v3.4'
├── manifest.json      ← "version": "3.4"
└── VERSION_GUIDE.md   ← המסמך הזה
```

---

**עודכן לאחרונה: 05/02/2026**
**גרסה נוכחית: 3.4**
